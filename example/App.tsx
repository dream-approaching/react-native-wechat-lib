import React, { FC, useRef } from 'react';
import { View, Text } from 'react-native';

export const App: FC<any> = () => {
  return (
    <View style={{ flex: 1 }}>
      <Text>123</Text>
    </View>
  );
};

export default App;
